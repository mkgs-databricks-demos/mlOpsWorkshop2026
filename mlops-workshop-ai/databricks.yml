# Declarative Automation Bundle configuration
# Bundle 2 of 3: Model Serving Endpoint + AI Gateway
bundle:
  name: mlops-workshop-ai

variables:
  catalog:
    description: "Must match -infra catalog"
    default: "mlops_workshop"
  user_schema:
    description: "Must match -infra schema"
  registered_model_name:
    description: "Full 3-level name of registered model from -infra"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://fevm-hls-fde.cloud.databricks.com
    variables:
      catalog: mlops_workshop
      user_schema: "user_${bundle.user_name}"
      registered_model_name: "mlops_workshop.user_${bundle.user_name}.churn_model"

  prod:
    mode: production
    workspace:
      host: https://fevm-hls-fde.cloud.databricks.com
      root_path: /Users/matthew.giglia@databricks.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: mlops_workshop
      user_schema: mlops_prod
      registered_model_name: "mlops_workshop.mlops_prod.churn_model"
    run_as:
      user_name: matthew.giglia@databricks.com
